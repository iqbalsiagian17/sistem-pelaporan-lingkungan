import 'package:flutter/material.dart';

class TermsDataEmptyState extends StatelessWidget {
  const TermsDataEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No data available"),
    );
  }
}
