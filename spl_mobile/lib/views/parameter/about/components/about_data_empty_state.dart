import 'package:flutter/material.dart';

class AboutDataEmptyState extends StatelessWidget {
  const AboutDataEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No data available"),
    );
  }
}
