import 'package:flutter/material.dart';

class ReportGuidesDataEmptyState extends StatelessWidget {
  const ReportGuidesDataEmptyState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tidak ada panduan pelaporan yang tersedia."),
    );
  }
}
